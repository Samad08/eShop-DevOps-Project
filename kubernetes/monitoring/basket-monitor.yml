apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: basket-api-monitor
  labels:
    release: monitoring # This MUST match the name of your Helm release
spec:
  selector:
    matchLabels:
      app: basket # Matches the label in your Catalog deployment
  endpoints:
  - port: http
    path: /metrics